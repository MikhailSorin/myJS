const flip = (d, a) => {
  if (d == "R") {
    for (let i = a.length; i >= 0; i--) {
      if (a[i - 1] > a[i]) {
        //сыпимся в право
      }
      const element = array[i];
    }
  } else if (d == "L") {
    for (let i = 0; i < a.length; i++) {
      if (a[i]) {
      }
      const element = array[i];
    }
  }
  return;
};
